{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "chatf16", 
            "type": "lldb",
            "request": "launch", 
            "program": "${workspaceRoot}/target/debug/learning-lm-rust.exe", 
            "args": ["chat"], 
            "stopAtEntry": false, 
            "cwd": "${workspaceRoot}",
            "environment": [],
            "preLaunchTask":"build",
           
        },
    ]
}